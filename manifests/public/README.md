# Public Manifests

Files here contain only factual compatibility metadata such as hashes, sizes,
version identifiers, and counts. They must never contain extracted paths lists
that reveal expressive game content, binary data, disassembly, generated code,
keys, secrets, or developer-specific absolute paths.

